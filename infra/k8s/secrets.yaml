apiVersion: v1
kind: Secret
metadata:
  name: neon-void-secrets
  namespace: neon-void
  labels:
    app.kubernetes.io/part-of: neon-void
type: Opaque
stringData:
  JWT_SECRET: "REPLACE_WITH_ACTUAL_JWT_SECRET"
  DATABASE_URL: "REPLACE_WITH_ACTUAL_DATABASE_URL"
  REDIS_URL: "REPLACE_WITH_ACTUAL_REDIS_URL"
